import 'package:flutter/material.dart';

class CheckupsPage extends StatelessWidget {
  const CheckupsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Медицинские осмотры', style: TextStyle(fontSize: 24)),
    );
  }
}