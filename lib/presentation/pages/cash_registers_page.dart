import 'package:flutter/material.dart';

class CashRegistersPage extends StatelessWidget {
  const CashRegistersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Кассы', style: TextStyle(fontSize: 24)),
    );
  }
}